import styled from "styled-components";

const Modal = () => {
  return (
    <div>
      <h3>설정</h3>
      <ModalTitle>가나다라</ModalTitle>
    </div>
  );
};

const ModalTitle = styled.div`
  font-size: 1rem;
`;

export default Modal;
